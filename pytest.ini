[pytest]
# Add directories to ignore (optional)
# nocolor = true

# Specify the format for test output (optional)
addopts = -v --disable-warnings --maxfail=1

# Specify the root directory for pytest to discover tests
testpaths = 
    test_
